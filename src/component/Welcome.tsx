import { Grid, Typography } from "@material-ui/core";

type NewProps = {

};

const Welcome = (props:NewProps) => {
  return (
    <Grid
      container
      item
      direction="column"
      alignItems="center"
      justify="center"
      style={{ padding: "30px", minHeight: "93vh" }}
    >
      <Grid item>
        <Typography variant="h2">We Can Help You Succeed</Typography>
      </Grid>
    </Grid>
  );
};


export const ErrorPage = (props : NewProps) => {
  return (
    <Grid
      container
      item
      direction="column"
      alignItems="center"
      justify="center"
      style={{ padding: "30px", minHeight: "93vh" }}
    >
      <Grid item>
        <Typography variant="h2">Error 404</Typography>
      </Grid>
    </Grid>
  );
};

export default Welcome;
